export function validateUsername(username) {
    if (username.length < 3) {
        return "please enter a username at least 3 characters long"
    }
    // more vaidations...
    // if (...)
    // if (...)

    // if everything is ok return empty string
    return "";
}